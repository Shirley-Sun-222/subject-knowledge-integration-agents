# 实施进度

## 当前状态

- 已创建 FastAPI + React/Vite 项目骨架。
- 已实现 SQLite schema、教材解析、图谱抽取、跨教材整合、RAG、教师反馈、报告和 PDF API。
- 已实现 React SPA：上传、图谱、整合、RAG、对话、报告入口。
- 已建立领域上下文、Agent 架构说明、系统设计、需求分析、ADR 和 README 运行说明。
- 已安装 Python 与 Node 依赖，包含 pytest、sentence-transformers、FAISS、Playwright Chromium。
- 已加入扫描版 PDF OCR fallback、OCR 语言回退、LLM 调用失败降级和图谱构建章节数上限。
- 已确认 `book_samples/` 中有 4 本测试教材；其中 1 本扫描版 PDF 需要 OCR，不能只依赖 PyMuPDF 文本抽取。
- 已新增 `scripts/load_book_samples.py`，支持导入 `book_samples/`、跳过或重置同名样例、构建单本图谱、整合、RAG 索引和报告生成。
- 已修复前端图谱视图状态：构建单本图谱后不会被旧整合图谱覆盖，并显示当前画布来源、节点/边数量和章节截断状态。
- 已新增 `.dockerignore`，避免 Docker 构建上下文包含 `.venv`、`node_modules`、`data/`、`book_samples/` 和 PDF 教材。
- 已将 `Dockerfile` 重构为多阶段构建：Node 阶段构建前端，Python 阶段运行后端；后端镜像不再通过 apt 安装 `nodejs/npm`，并在镜像内安装 Tesseract OCR、中文字体和 Playwright Chromium。

## 验证结果

- `.venv/bin/python -m pytest backend/tests`：10 项通过。
- `.venv/bin/python scripts/load_book_samples.py --limit 1 --reset-samples --build-graphs --graph-max-chapters 3`：通过，样例 PDF 保留原始文件名，3 章生成 18 个节点、15 条边。
- `.venv/bin/python scripts/load_book_samples.py --reset-samples --build-graphs --ocr-max-pages 5 --graph-max-chapters 2`：4 本样例均 completed；扫描版 C++ 教材经 OCR 解析出 3 章；每本教材均生成 12 个节点、10 条边。
- `.venv/bin/python scripts/load_book_samples.py --integrate --index-rag --report --ocr-max-pages 5 --graph-max-chapters 2`：通过，整合 48 个节点、40 条边，压缩比约 1.20%，RAG 索引 1143 个 chunk，报告写入 `report/整合报告.md`。
- `.venv/bin/python scripts/run_rag_benchmark.py`：通过，5 个 benchmark 问题均返回 citation，当前仅作为引用链路 smoke test。
- `npm --prefix frontend run build`：通过。Vite 提示 Cytoscape 相关 chunk 超过 500 KB，当前不影响运行。
- PDF 导出：已生成 `data/generated/整合报告.pdf`。
- 浏览器首屏：`http://127.0.0.1:5173/` 可打开；Playwright smoke test 读取到 4 本教材、整合图谱 48 节点/40 边、RAG chunk 1143。控制台仅有 Vite/React 开发提示。
- Docker Desktop：CLI 可用；`docker compose build --progress plain` 已重试。当前阻塞在 Docker Hub 基础镜像 metadata：`node:20-bookworm-slim` 与 `python:3.11-bookworm-slim` 访问 `registry-1.docker.io` 超时，尚未进入 Dockerfile 的 apt、pip、npm 或 Playwright 安装阶段。本机仅缓存了 `python:3.11-slim`，不足以完成多阶段构建验证。

## 上下文治理

常规状态更新写入本文档，不需要用户确认。compact、handoff、切换 session、大规模重规划或放弃承诺 P1 项必须先向用户汇报当前进展并请求确认。

## 下一步

- 用真实 7 本教材跑完整流程并生成最终 `report/整合报告.md`。
- 网络恢复或配置 Docker Desktop 代理后，重新运行 `docker compose build` 和 `docker compose up` 完成容器本地验证；成功后继续验证 `/api/health`、前端静态资源、教材上传、单本图谱、整合、RAG、PDF 导出和 `data/`/`report/` 挂载持久化。
- 进一步提升 benchmark 质量：当前 `citation_accuracy_proxy` 只检查是否有引用，不能判断无关问题是否应拒答。
- 配置真实 LLM 环境变量后复测知识点抽取质量。
- 如需降低前端 bundle 体积，可对 Cytoscape 做动态加载。
